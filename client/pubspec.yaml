name: lentera
description: LENTERA mental health super app
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  flutter_sound: ^9.2.13
  web_socket_channel: ^2.4.0
  provider: ^6.0.5
  google_fonts: ^6.1.0
  flutter_animate: ^4.5.0
  supabase_flutter: ^2.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets: []
